// JavaScript (script.js)

// Wait for the window to finish loading
window.onload = function () {
    // Set a timeout for the splash screen
    setTimeout(function () {
        // Redirect to the main page
        window.location.href = "index.html"; // Cambia esto a la URL real de la página principal
    }, 5000); // Redirige después de 5 segundos (ajusta según sea necesario)
};
